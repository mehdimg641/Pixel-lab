# ممیزی ۱۵۰۰ ویژگی

`docs/FEATURE-AUDIT-1500.md` **ساخته می‌شود، دستی نوشته نمی‌شود.**

```
python3 tools/audit/audit.py
```

## دو ورودی

| فایل | چیست |
|---|---|
| `features-1500.txt` | فهرست ۱۵۰۰ ویژگیِ خودتان، دست‌نخورده. نامِ هر ویژگی و نامِ هر بخش از اینجا می‌آید. |
| `audit.py` | **وضعیت** هر ویژگی، در حدود ۸۰۰ فراخوانی `rule()`. |

هر دو تا امروز بیرون از مخزن بودند — یکی در پوشهٔ موقت جلسه و یکی در پوشهٔ آپلود — و هیچ‌کدام از یک
جلسه بیرون نمی‌آمد. یعنی سندی ۱۶۰۰ خطی داشتیم که کسی نمی‌توانست بازتولید یا تصحیحش کند.

## چهار نشانه

| نشانه | معنی |
|---|---|
| ✅ | هست **و از رابط کاربری در دسترس است** |
| 🟡 | نیمه — مدلش هست و چیزی روی صفحه به آن نمی‌رسد، یا با دو-سه گام دیگر شدنی است |
| ❌ | نیست |
| ⛔ | عمداً بیرون از دامنه: ویدیو، ابر، شبکهٔ اجتماعی، مدل هوش مصنوعی سروری |

**تفکیک ✅ از 🟡 تمام هدف این کار است.** داشتنِ مدل با داشتنِ ابزار یکی نیست، و تجربهٔ همین مخزن
بارها نشان داده که فاصلهٔ واقعی ما تقریباً همیشه در *سیم‌کشی* است نه در مدل: اریب، پرسپکتیو، هشت
افکت CPU، پرکنندهٔ لایه، جدول رنگ، محو سطحی، نقشهٔ گرادینت و عمق بیت — همه در مدل بودند و هیچ
کنترلی به آن‌ها نمی‌رسید. این بدتر از نبودن است، چون از بیرون شبیه بودن به نظر می‌رسد.

قاعدهٔ عملی: پیش از نوشتن ✅، نشانی‌ای بدهید که `rg` آن را در `app/android/` یا در یک `*Sheet.kt`
پیدا کند — نه فقط در `core/`.

## قاعده‌ها

```python
rule((1, 250), MISSING, "")                    # بازهٔ بسته: ۱ تا ۲۵۰
rule([3, 4, 5], HALF, "موتورش هست…")           # چند شماره
rule(7, DONE, "PatchMatch + `healSelection`")  # یکی
```

**قاعدهٔ بعدی برنده است.** هر بخش با یک پیش‌فرضِ فراگیر شروع می‌شود و بعد استثناها را روی آن
می‌نویسد. توضیح هر ردیف باید نامِ سمبلی را ببرد که ادعا را پشتیبانی می‌کند.

## چه چیزی را نمی‌گیرد

اسکریپت **به کد نگاه نمی‌کند** — `import` آن فقط `collections, os, re, sys` است. تنها چیزی که
خودکار است، شمارش و درصد و قالب‌بندی مارک‌داون است؛ خودِ قضاوت‌ها را آدم می‌نویسد.

نگهبانِ `sys.exit(1)` فقط ویژگیِ بی‌قاعده را می‌گیرد. **وضعیتِ غلط را نمی‌گیرد** — برای آن باید کد را
خواند.
