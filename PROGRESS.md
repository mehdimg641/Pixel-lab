# PROGRESS

وضعیت در برابر نقشهٔ راه سند (§۱۴). این فایل در پایان هر جلسه به‌روز می‌شود.

## فاز صفر — بنیاد

| # | تحویل‌دادنی | وضعیت | یادداشت |
|---|---|---|---|
| ۰.۱ | مدل سند + سریال‌سازی | ⚠️ جزئی | کانتینر ZIP کار می‌کند؛ مدل لایه‌ای است نه DAG |
| ۰.۲ | موتور رستر GPU، ۳۲ بیت خطی | ⚠️ جزئی | GL کار می‌کند؛ بدون کاشی‌بندی و بدون ۳۲ بیت |
| ۰.۳ | Undo/History | ⚠️ جزئی | خطی و کامل؛ بدون شاخه‌بندی |
| ۰.۴ | موتور متن فارسی | ⚠️ جزئی | درست رندر می‌شود، از استک اندروید؛ HarfBuzz مستقیم نیست |
| ۰.۵ | یکی‌سازی بولین + مثلث‌بندی | ✅ | گوش‌بری با پل حفره؛ بولین در `core:vector` |
| ۰.۶ | موتور سه‌بعدی + IBL | ⚠️ جزئی | PBR و محیط تحلیلی؛ بدون Filament و بدون HDRI |
| ۰.۷ | موتور قلم | ✅ | نوک، فاصله، فشار، نرم‌سازی، پراکندگی، بافت |
| ۰.۸ | اسکلت رابط، RTL، تم کارگاه | ✅ | این جلسه |
| ۰.۹ | طراحی لایهٔ انتزاع AI | ✅ | `core:ai` — قرارداد + مسیر کلاسیک |

## فاز یک — ویرایشگر عکس و متن دوبعدی

| بخش | وضعیت |
|---|---|
| ۱.الف لایه و ساختار | ✅ ۲۷ حالت ترکیب، ماسک، Layer Styles، Blend If، Layer Comps |
| ۱.ب تنظیم رنگ | ✅ ۲۲ لایهٔ تنظیم |
| ۱.پ موتور قلم | ⚠️ بدون نقاشی متقارن و ایمپورت ABR |
| ۱.ت رتوش و ترمیم | ✅ ترمیم، مهر کپی، و پرکردن آگاه‌به‌محتوا (`healSelection` با PatchMatch) |
| ۱.ث انتخاب و ماسک | ✅ شامل پالایش لبه |
| ۱.ج تبدیل | ⚠️ وارپ و پرسپکتیو هست؛ Puppet Warp نیست |
| ۱.چ فیلتر | ⚠️ لیکوییفای هست (هل، باد، جمع، پیچش)؛ نسخهٔ آگاه‌به‌چهره و گالری هنری/محو نیست |
| ۱.ح متن دوبعدی | ✅ |
| ۱.خ سند و کارگاه | ⚠️ بدون Artboards |
| ۱.د ورودی/خروجی | ⚠️ بدون TIFF/AVIF/EXR |
| ۱.ذ پایهٔ برند | ❌ کیت برند چند-مشتری نیست |

## فاز دو — تایپوگرافی سه‌بعدی

| # | تحویل‌دادنی | وضعیت |
|---|---|---|
| ۲.۱ | اکسترود + کَپ + بِوِل + **محافظ خودکار شعاع فارسی** | ✅ محافظ کامل شد |
| ۲.۲ | متریال PBR + کتابخانه | ⚠️ متریال هست، کتابخانهٔ ۸۰تایی نیست |
| ۲.۳ | نورپردازی + IBL/HDRI + Shadow Catcher | ⚠️ نور و IBL هست (`LatLong` با میپ پیش‌فیلترشده)؛ Shadow Catcher نیست |
| ۲.۴ | دوربین فیزیکی + تطبیق پرسپکتیو با عکس | ❌ |
| ۲.۷ | نوار خوشه‌ها + کنترل مستقل نقطه و اعراب | ✅ هر دو کامل شد |
| ۲.۱۰ | مسیریاب پرتو | ❌ |

**۲.۱ بسته شد.** `BevelGuard` ضخامت محلی هر نقطه از کانتور را با پرتاب پرتو در جهت نرمال داخلی
اندازه می‌گیرد و شعاع بِوِل را به نصف آن محدود می‌کند. این همان موردی است که سند در §۱۵ «قطعی اگر
حل نشود» علامت زده بود.

## این جلسه

**ساخته شد.** سیستم طراحی «کارگاه» دقیقاً بر §۶: پالت کامل تیره و روشن، Vazirmatn Variable
بسته‌بندی‌شده، مقیاس تایپ پنج‌نقشی با `tnum`، فاصله و شعاع و حرکت. ساختار §۱۳.۵: نوار تاریخچهٔ
۵۶dp با پرش به گام، ریبون بافتار ۸۸dp، داک ۶۴dp پنج‌تایی. حذف گرادیان تزئینی، آیکون‌های تک‌رنگ
خطی، ارقام لاتین در فیلد فنی و فارسی در متن.

**تست شد.** `./gradlew build` سبز. تست‌های تصویری: خانه (پر و خالی)، خانه در تم روشن، کروم
ویرایشگر، اجزای مشترک، کنترل‌های شیت، هیستوگرام، انتخابگر رنگ، ویرایشگر گرادیان.

**تصمیم گرفته شد.** ده مورد در `DECISIONS.md`، از جمله چهار انحراف علامت‌دار از سند.

**باقی ماند.** بدهی‌های علامت‌دار در `DECISIONS.md` §۱ تا §۴ و §۱۰ — به‌ویژه HarfBuzz مستقیم و
انتقال رشته‌ها به `strings.xml`.

**جلسهٔ بعد.** پیشنهاد: کنترل مستقل نقطه‌ها و اعراب (§۶.۹.۳) — همان واحد خوشه را می‌گیرد و عمق و
متریال جداگانه به نقطه‌ها می‌دهد. یا تسویهٔ بدهی HarfBuzz (`DECISIONS.md` §۲)، که سند اولویتش
می‌دهد.

---

## جلسهٔ کیفیت خروجی سه‌بعدی

**شروع از یک ایراد درست کاربر.** رندر نمونه بی‌کیفیت بود و این را نباید با «تست است» رد کرد.

**اول سه فرضیهٔ غلط خودم، که با اندازه‌گیری رد شدند.** (۱) پلکانی بودن لبه از ضدلبه‌دندانه است — نبود:
رندر با ۱، ۲ و ۴ نمونه در هر محور تقریباً یکسان درآمد، و ردیابی پیکسل‌به‌پیکسل نشان داد لبه درست
ضدلبه‌دندانه شده (آلفا ۶۳، ۱۲۷، ۱۹۱، ۲۵۵ و لبه در طول ۲۸۰ سطر تنها ۵ پیکسل جابه‌جا). (۲) هندسه کج
است — نبود: نقاط لبهٔ چپ دقیقاً `x=15.000` بودند. (۳) مشکل از فیکسچر بلوکی است — تنها بخشی از آن.

**آزمون قطعی: رندر حروف واقعی از فونت واقعی، از همان مسیری که خود اپ صدا می‌زند.**
`DimensionalRenderTest` در `engine:android` با Vazirmatn بسته‌بندی‌شده «کاربیست» و «بیست» را
می‌سازد. تصویر بلافاصله دو چیز را گفت.

**خبر خوب:** تشخیص نقطه روی حروف واقعی دقیقاً کار می‌کند — سه نقطهٔ زیر خط (ب و ی) و دو نقطهٔ روی
خط (ت) کروم و شناور شدند و هیچ‌چیز دیگری نشد.

**نقص واقعی:** حروف مثل روبان تاشده درمی‌آمدند نه قلمِ توپر — یعنی دقیقاً همان شکستی که `BevelGuard`
برای جلوگیری از آن نوشته شده بود. دو علت، هر دو اصلاح شد:

۱. `BEVEL_FRACTION` پیش‌فرض اپ ۰٫۰۴ بود. قلم فارسی نسبت به اندازه‌ای که نامش را می‌برد باریک است؛
Vazirmatn در ۲۶۰ پوینت ساقی حدود یک‌دهم آن می‌کشد، پس ۰٫۰۴ یعنی بِوِلی به اندازهٔ یک‌سوم قلم، از
*هر طرف*. حالا ۰٫۰۱.

۲. `SAFETY` محافظ ۰٫۷ بود، یعنی اجازه می‌داد بِوِل ۷۰٪ هر قلم را بخورد و تنها ۳۰٪ رویه بماند. از
نظر هندسی ایمن بود و از نظر بصری نبود. حالا ۰٫۵، پس هر قلم نصف عرضش را به‌عنوان رویه نگه می‌دارد.

**نگهبان بازگشت.** تست شمارش پیکسل رویه دارد و **آزموده شد که با مقادیر قدیمی می‌افتد** — یک ادعا که
هرگز نمی‌افتد بی‌ارزش است.

---

## جلسهٔ کنترل مستقل نقطه‌ها و اعراب

**ساخته شد.** `core:mesh/Ornaments` — تشخیص اینکه کدام کانتور یک نقطه یا اعراب است و کدام تنهٔ حرف،
تنها از روی هندسه، چون در طرح‌کلی حرف هیچ ابرداده‌ای نیست که بگوید کدام کدام است. `MarkStyle` عمق و
جلوآمدگی نقطه را به‌صورت ضریبی از عمق تنه نگه می‌دارد، `Mesh` یک پرچم به‌ازای هر مثلث دارد،
`Rasteriser` متریال جدای نقطه را می‌خواند، و پنل سه‌بعدی سه پیش‌تنظیم «همسطح / فرورفته / شناور»
به‌علاوهٔ دو لغزنده و یک متریال اختیاری نشان می‌دهد.

**آزمون سخت، تو در تو بودن است نه کوچکی.** حفرهٔ «ه» و «و» و «ق» هم کوچک است و هم آزاد از خط بیرونی؛
اگر با نقطه اشتباه گرفته و شناور شود، وسط حرف در هوا می‌ماند. پس اول آزمون در بر گرفتن اجرا می‌شود و
تنها بعد از آن اندازه — یک حفره هرقدر هم کوچک باشد هرگز نقطه نیست.

**تست شد.** `OrnamentsTest` (نه تست) و یک رندر مقایسه‌ای در `RenderSampleTest`. `./gradlew build` سبز.

**دو انتظار غلط در تست، نه در کد.** (۱) شمارش مثلث‌های پرچم‌دار بی‌معناست: حلقه‌ها به‌ازای هر نقطهٔ
کانتور دوخته می‌شوند، پس یک نقطهٔ چهارگوش و یک تنهٔ چهارگوش دقیقاً به یک تعداد مثلث می‌رسند — جای
مثلث‌ها آزمون شد، نه شمارشان. (۲) شمارش پیکسل خنثی برای اثبات رسیدن متریال هم بی‌معنا بود: رویهٔ حرف
سفید براق است و آن هم خنثی است، و از آن بدتر، مِش شناور عمیق‌تر است و دوربین خودش را به مِش قاب
می‌کند، پس *هر* شمارش سطحی بین دو رندر کمی افت می‌کند. آنچه ماند: خاکستریِ میانه، رنگی که در پالت
طلا-و-سفید این صحنه هیچ منبعی جز کروم ندارد — صفر در رندر همسطح، ۳۳۴ در رندر شناور.

---

## جلسهٔ نوار خوشه‌ها

**ساخته شد.** `core:text/Clusters` — واحد «خوشهٔ متصل» با پنج سطح جداسازی، و
`app/GlyphRibbon` — یک چیپ به‌ازای هر خوشه، انتخاب با ضربه، و **کشیده با کشیدن دستگیرهٔ چیپ**.
نیم‌فاصله به‌صورت نشانهٔ کهربایی دیده می‌شود (ایدهٔ ۸ سند). نوار روی داک‌های «متن» و «سه‌بعدی»
جای ریبون ابزار را می‌گیرد، نه روی داک «عکس».

**تست شد.** `ClustersTest` با متن‌های اجباری §۹ — «سلام» دو خوشه، «دارد» چهار، نیم‌فاصله محترم،
اعراب با حرف خودش، لیگاچر «لا» نشکسته، و لاتین با فارسی قاطی نمی‌شود. خاصیت کلیدی: پیوستن دوبارهٔ
تکه‌ها همیشه دقیقاً متن اصلی را می‌دهد. `./gradlew build` سبز.

**دو تصحیح در انتظار من، نه در کد.** انتظار داشتم «رود» یک خوشه باشد و «مدیا» هم — هر دو غلط بود:
«ر» و «و» به بعدی وصل نمی‌شوند، پس «رود» سه شکل جداست و «مدیا» دو خوشه. کد از اول درست بود؛ تست
اصلاح شد.

---

## جلسهٔ محافظ شعاع بِوِل

**ساخته شد.** `core:mesh/BevelGuard` — اندازه‌گیری ضخامت محلی با پرتاب پرتو در جهت نرمال داخلی، و
محدودسازی شعاع بِوِل در هر نقطه به نصف آن با ضریب ایمنی. `Extruder` حالا بِوِل را با عرض متغیر
می‌سازد به‌جای یک عرض ثابت برای کل حرف.

**تست شد.** `BevelGuardTest` (شش تست) و یک رندر نمونهٔ مقایسه‌ای در `RenderSampleTest`.
`./gradlew build` سبز.

**سه مسیر بن‌بست، که در کد ثبت شده‌اند.** (۱) اندازه‌گیری با تبدیل فاصله به‌جای وتر — شعاع دایرهٔ
محاطی را می‌دهد که جهت‌دار نیست، و هر گوشهٔ هر حرف را کم‌بِوِل می‌کرد. (۲) بازنمونه‌برداری خود
کانتور برای اینکه محافظ جایی برای عمل داشته باشد — افست گوشهٔ محدب را روی خودش تا می‌کند و مثلث‌بندی
از کار می‌افتد. (۳) تخمین محیط بسته از میانگین گام‌ها — فاصلهٔ دور زدن منفی می‌شود و شعاع منفی
درمی‌آید. راه‌حل نهایی: **اندازه‌گیری متراکم، اعمال روی همان نقاط اصلی.**

---

## جلسهٔ فهرست کامل Adjustments و افکت‌هایی که هرگز کشیده نمی‌شدند

**ساخته شد — شش تنظیم باقی‌ماندهٔ فتوشاپ.** از بیست‌ودو مورد منوی Adjustments، شانزده‌تا بود.
شش‌تای نبوده دقیقاً همان‌هایی‌اند که خود فتوشاپ به‌عنوان *لایهٔ* تنظیم ارائه نمی‌دهد، و این تصادفی
نیست: `ShadowsHighlights` و `HdrToning` همسایگی پیکسل را می‌خوانند، `Equalize` و `MatchColor`
به اندازه‌گیری کل تصویر نیاز دارند، و هیچ‌کدام را ترکیب‌کننده‌ای که یک تکسل می‌بیند نمی‌تواند حساب
کند. در اپ ما همه لایه‌اند: همسایگی یک پاس محو است، و اندازه‌گیری یک‌بار گرفته و در خود لایه
منجمد می‌شود — که نتیجه را *پایدار* هم می‌کند.

**انطباق موضعی HDR با فیلتر هدایت‌شدهٔ واقعی روی GPU**، نه گاوسی: در دو برنامهٔ کوچک به‌علاوهٔ همان
محوی که بود، و ضرب‌وجمع پایانی داخل خود شاخهٔ تنظیم جمع می‌شود، پس پاس سومی لازم نیست. پایهٔ گاوسی
از روی هر لبهٔ تصویر رد می‌شود و فشرده‌کردنش هالهٔ روشنی می‌گذارد که امضای کار tone-mapped بد است.
راهنما لگاریتم *نرمال‌شدهٔ* روشنایی است، و این نرمال‌سازی تزئینی نیست.

**اوراکل CPU برای هر بیست‌ودو تنظیم.** تنظیم‌های اپ یک برنامهٔ GLSL با یک شاخه به‌ازای هر حالت
است، و تا امروز هیچ تستی در پروژه بررسی نمی‌کرد آن برنامه *چه* حساب می‌کند — فقط اینکه اعداد درست
به شکاف‌های یونیفرم درست می‌رسند. شیدری که چیز غلط حساب کند یونیفرم‌هایش را بی‌نقص بسته‌بندی می‌کند.

**هشت افکتی که ترکیب‌کنندهٔ CPU اصلاً نمی‌کشید.** از چهارده افکت، شش‌تا پیاده بود و بقیه بی‌صدا رد
می‌شدند — و پرکنندهٔ خود لایه هرگز اعمال نمی‌شد. یعنی هر چهرهٔ گرادینتی در کتابخانه با رنگ سیلوئت
(معمولاً سفید) درمی‌آمد، در حالی که افکت‌های دورش درست کشیده شده بودند. هر افکت به‌تنهایی درست بود
و تصویر غلط.

**هشت پریست تازه** برای خانواده‌هایی که کتابخانه نداشت: کروم، فلز کشیده، آتش، گلیچ، گرافیتی، اکلیل،
رترو هشتاد، سایبرپانک. کروم به منحنی براقیت *حلقه‌ای* نیاز داشت — روشن، تیره، روشن روی یک شانه —
چون شانهٔ یکنواخت یک هایلایت دارد و پلاستیک نورخورده خوانده می‌شود.

**تست شد.** `AdjustTest` روی مقدارها ادعا می‌کند نه روی تصویر، و اولین تستش این است که هر تنظیم در
حالت پیش‌فرض همانی باشد. `StyleLibraryRenderTest` هر پریست کتابخانه را رندر می‌کند و هرکدام را که
شبیه متن خام دربیاید رد می‌کند — همین تست ساده نیمی از کتابخانه را گرفت. `./gradlew build` سبز.

**باقی ماند.** اریب‌کردن (skew) بعد از تبدیل به آبجکت هوشمند، و پس‌زمینهٔ دانه‌دار با وینیت — دو
مورد باقی‌ماندهٔ دستور فتوشاپی. و بدهی HarfBuzz (`DECISIONS.md` §۲).

## جلسهٔ سیم‌کشی، و سی تصویر از AirBrush

**الگویی که هشت بار تکرار شد.** هر بار که این جلسه دنبال کمبود گشت، همان شکل را پیدا کرد: مدل
چیزی را بیان می‌کند و هیچ کنترلی به آن نمی‌رسد. اریب، پرسپکتیو، هشت افکت CPU، پرکنندهٔ لایه،
جدول رنگ، محو سطحی، عمق بیت، و نقشهٔ محیطی. این بدتر از نبودن است چون از بیرون شبیه بودن است، و
قاعده‌ای که از آن درآمد حالا در `tools/audit/README.md` نوشته شده: پیش از نوشتن «هست»، نشانی‌ای
بده که کاربر بتواند فشارش دهد.

**ممیزی ۱۵۰۰ ویژگی وارد مخزن شد.** تا این جلسه، سندِ ۱۶۰۰ خطیِ `docs/FEATURE-AUDIT-1500.md` از
دو فایل بیرون از مخزن ساخته می‌شد که هیچ‌کدام از یک جلسه بیرون نمی‌آمدند — یعنی کسی نمی‌توانست
بازتولید یا تصحیحش کند. حالا `tools/audit/` است و بازسازی یک فرمان است.

**نقشهٔ محیطی، که مدل نامش را برده بود و کسی نمی‌خواند.** `LatLong` یک عکس equirectangular
واقعی را می‌گیرد و زنجیرهٔ میپ در نور خطی می‌سازد؛ زبری بین دو سطح میان‌یابی می‌کند، که همان
کاری است که GPU با کیوب‌مپِ پیش‌فیلترشده می‌کند و به همان دلیل. زنجیره تا تک‌تکسل پایین می‌رود،
نه تا ۲×۱ — وگرنه درشت‌ترین سطح هنوز یک نیمهٔ چپ و یک نیمهٔ راست دارد و فلزِ کاملاً زبر به‌جای
میانگینِ اتاق باز هم جهت را بازتاب می‌دهد.

**سی تصویر از AirBrush، صفحه به صفحه.** `docs/REFERENCE-AIRBRUSH.md` هر صفحه و هر کنترلش را
کنار آنچه ما داریم می‌گذارد. خط جداکننده روشن درآمد: هر چیزی که «کجا»یش را کاربر می‌گوید، ما
داریم و بهترش را داریم؛ هر چیزی که اپ خودش «کجا» را پیدا می‌کند، به مدل نقاط چهره نیاز دارد که
تصمیمش با کاربر است. و دو کمبود پیدا شد که هیچ فهرست ویژگی نمی‌گیردشان چون دربارهٔ *چیدمان*اند.

**برش با نسبت‌های آماده.** `cropCanvas(rect)` از موج ۱ رکتانگل می‌گرفت و تنها رکتانگلی که کسی
می‌توانست بدهد کادرِ انتخاب بود — پس پستِ مربع و استوریِ ۹:۱۶ باید با چشم کشیده می‌شد.
`AspectRatio` روی مارکیِ مستطیلی سوار شد، همان‌جا که فتوشاپ هم Fixed Ratio را گذاشته.
`constrain` کوچک می‌کند و هرگز بزرگ نمی‌کند، وگرنه قاب از زیر انگشت بیرون می‌پرد؛ و مهارِ لبه یک
انتقال است نه تغییر شکل دوم، پس قابی که به گوشه فشرده شود روی لبه سُر می‌خورد و نسبتش را از دست
نمی‌دهد.

**مقایسهٔ قبل و بعد.** با راه‌رفتن روی تاریخچه تا صفر و برگشت، نه با نگه‌داشتن یک عکس — چون عکسِ
سند دروغ می‌گفت: پیکسل‌های نقاشی‌شده روی رستر دارایی می‌نشینند و کنترلر نقاشی دوباره اعمالشان
می‌کند. `jumpTo` هر دو نوع گام را بازپخش می‌کند، پس مقایسه نمی‌تواند از واگرد فاصله بگیرد.

**و طرحِ مارکی یک قطر بود.** کشیدن کادر، خطِ بین دو گوشه را می‌کشید نه خودِ کادر — و زیر نسبتِ
ثابت اصلاً درست نیست، چون گوشه‌ای که کشیده می‌شود دیگر جای انگشت نیست.

**اسناد تسویه شد.** `docs/GAP-ANALYSIS.md` در برابر درخت امروز بازنویسی شد (نسخهٔ قبلی وکتور و
قلم‌مو و انتخاب پیکسلی را ۰ می‌داد و هر سه ساخته شده‌اند)، `docs/STATUS.md` به‌عنوان تاریخ
علامت خورد، و جدول‌های فاز در همین فایل تصحیح شدند.

**تست شد.** `CropTest`، `EnvironmentTest`، `CubeLutTest`، `AdjustTest`، و شش تست تازه روی
`SelectionControllerTest`. `./gradlew build` سبز.

**باقی ماند، به ترتیب.** تأیید/انصراف صریح روی هر شیت؛ جفت رنگ پیش‌زمینه/پس‌زمینه؛ قلم‌موهای
دوج و برن و انگشتی و اسفنجی؛ Pathfinder؛ گالری محو با شکل دیافراگم؛ انتخاب سریع و ماسک سریع.
و بدهی HarfBuzz (`DECISIONS.md` §۲).

**پس‌نوشت همان جلسه — شیت به‌عنوان تراکنش.** صدرِ فهرست بالا بلافاصله بسته شد. سرِ هر شیت حالا
✕ و عنوان و ✓ دارد، و انصراف سند را به لحظهٔ باز شدنِ شیت برمی‌گرداند. یک قید عمدی دارد که
مهم‌تر از خود قابلیت است: **انصراف روی ضربه‌های قلم‌مو نمی‌رود.** ضربه‌ای که کاربر با شیتِ
*تنظیماتِ* قلم‌مو باز زده کارِ خودِ اوست نه کارِ شیت، و انصرافی که آن را بخورد از نبودنِ انصراف
بدتر است. پس تا اولین گامِ نقاشی عقب می‌رود و همان‌جا می‌ایستد؛ بقیه‌اش با واگرد در دسترس است و
هیچ‌چیز از بین نمی‌رود.

## جلسهٔ هایپیک — سه فیلتری که فقط یک قدم دور بودند

**اول یک تصحیح در خودِ فهرست.** بخش ۵ ممیزی «های پیک (HiPaint)» نام دارد و صد ردیفش دربارهٔ
**HiPaint** است — اپ طراحی و اسکچ. اپی که کاربر نشان داد **Hypic** است، ویرایشگر عکس. دو اپ
متفاوت با یک نام فارسی، و یعنی هایپیک تا امروز هیچ ردیفی در سند ۱۵۰۰تایی نداشته.
`docs/REFERENCE-HYPIC.md` جایش را می‌گیرد.

**ردیف Adjust هایپیک بیست‌وپنج کنترل دارد.** بیشترشان را داشتیم — و سه‌تا را نه، و هر سه یک قدم
از چیزی بودند که از قبل ساخته و تست شده بود:

**مه‌زدایی، با پیشینهٔ کانال تاریک** (He، Sun و Tang، CVPR 2009). این تنها کنترلِ آن ردیف است
که منحنی نمی‌تواند تقلیدش کند: مه یک منحنیِ تُن نیست، حجابی است که با **فاصله** غلیظ‌تر
می‌شود، و هر چیزی که یکنواخت اعمال شود یا دورها را شیری می‌گذارد یا نزدیک‌ها را له می‌کند.
عبورپذیریِ درآمده از پیشینه بلوکی است و پالایشش دقیقاً کاری است که `GuidedFilter` برای آن نوشته
شده — همان که برای انطباق موضعی HDR داشتیمش.

**کنتراست موضعی در سه اندازه.** پنج اسلایدر با پنج نام مختلف در اپ‌های مرجع (Clarity، Texture،
Brilliance، Structure، Definition) یک عمل‌اند با شعاع متفاوت، و یک‌بار گفتنش از پنج پیاده‌سازی
بهتر است. دو چیز آن را از یک نمایش به یک ابزار تبدیل می‌کند: روی **روشنایی** کار می‌کند و رنگ را
دست‌نخورده برمی‌گرداند (فشار دادن در RGB اشباع را با خودش می‌کشد و همان ظاهرِ HDRِ بدپخت را
می‌سازد)، و بهره‌اش در دو سرِ بازه صفر می‌شود — چون پیکسلی که از قبل به سیاه یا سفید رسیده جایی
برای بزرگ‌شدنِ جزئیاتش ندارد، و بزرگ کردنش دقیقاً همان کاری است که هالهٔ سیاه دور خط آسمان
می‌سازد.

**پرتوهای نور (Tyndall).** بازاستفادهٔ بدیهی `RadialBlur` با `ZOOM` بود و **غلط** — آن فیلتر
بردش را روی چهل درصد شعاع هر پیکسل می‌بندد (که برای زوم‌بلور درست است، چون بلندتر یعنی لرزش
دوربین) و نمونه‌هایش را یکنواخت میانگین می‌گیرد، پس پرتو ته مسیر به همان روشنیِ سرِ مسیر
می‌ماند. پس انباشتگر خودش را دارد: از پیکسل به سمت منبع راه می‌رود و در هر گام یک میرایی ضرب
می‌کند. افت با فاصله از خودِ راه‌رفتن درمی‌آید نه از یک ضریب بیرونی — پیکسل دور، کسر کمتری از
مسیرش داخل هایلایت است.

**و یک ادعای غلط خودم که تصحیح شد.** در سند ایربراش نوشتم «شکل دیافراگم نداریم». `LensBlur`
پرهٔ دیافراگم دارد (۵، ۶، ۸) با وزن‌دهی هایلایت، و `GradientBlur` تیلت‌شیفت و بوکهٔ پرتره را —
هر دو از شیت فیلتر در دسترس. از گالری محوِ فتوشاپ فقط محو مسیری کم است.

**تست شد.** `AtmosphereTest` — شانزده ادعا روی خاصیت، نه روی تصویر مرجع: مه‌زدایی باید بیشترِ
کارش را جایی بکند که مه هست، عکسِ بی‌مه باید تقریباً دست‌نخورده بماند، کنتراست موضعی نباید لبه
را جابه‌جا کند یا اشباع را بکشد یا از سیاه و سفید رد شود، و نور باید فقط اضافه کند.
`./gradlew build` سبز.

## جلسهٔ مدل چهره، شش قلم‌موی تُنی، و یک ادعای غلط دیگر

**دو تصمیمی که گفته بودم با شماست، گرفته شدند.** مدل نقاط چهرهٔ MediaPipe — Apache 2.0،
۳٫۷ مگابایت — دانلود شد و **داخل خودِ APK** بسته‌بندی شد، نه دانلودِ اولین‌بار: اپی که ادعای
آفلاین بودن دارد نمی‌تواند قابلیتی داشته باشد که اولین استفاده‌اش به شبکه نیاز دارد. و RAW:
مانع هیچ‌وقت پروانهٔ LibRaw نبود — LGPL برای اپ متن‌باز مشکلی ندارد — مانع ساختِ NDK برای
چهار معماری بود، پس مسیر DNG انتخاب شد.

**همان شکافِ همیشگی، از سمت مقابل.** هر روتوش خودکارِ سه اپ مرجع دو گام است: ناحیه را پیدا
کن، بعد یک عمل پیکسلی اجرا کن. ما **همهٔ** عمل‌های پیکسلی را داشتیم و هیچ‌کدام از پیداکردن‌ها
را. حالا `core:ai` هندسه را دارد — توپولوژی ۴۷۸ نقطه، کانتورِ هر ناحیه، ماسک با لبهٔ نرم — و
با یک صورتِ مصنوعیِ دست‌ساز در تست اجرا می‌شود؛ نیمهٔ دیگر سی خط در `engine:android` است.

**چیزی که آرایش دیجیتال را از یک لکهٔ رنگی جدا می‌کند** یک جمله است و در `FaceRetouch` بالای
همه نوشته شده: بافتِ خودِ عکس باید زنده بماند. رژی که رنگ را *جایگزین* کند برقِ لب را صاف
می‌کند و فوراً استیکر می‌شود. سفیدکردنِ دندان هم اول اشباع را می‌گیرد و بعد روشنایی را
بالا می‌برد، چون آنچه چشم «سفیدتر» می‌خواند بیشتر *حذفِ زردی* است تا روشنایی.

**تغییر شکل صورت = نقاط کنترلِ همان وارپی که از موج ۵ داشتیم** و فقط با انگشت رانده می‌شد.
صُلب، همیشه — حالت افاین کشیدگی می‌دهد و گونهٔ کشیده امضای هر پرترهٔ بیش‌ازحد روتوش‌شده است.
و **نقاط مهارشده به‌اندازهٔ نقاط متحرک مهم‌اند**: بدون مهارِ خط چشم و قابِ تصویر، باریک‌کردنِ
چانه بی‌سروصدا پیشانی را هم باریک می‌کند و پس‌زمینه سُر می‌خورد.

**شش قلم‌مویی که به‌جای نقاشی، تصویر را می‌خوانند.** دوج، برن، اسفنجی، انگشتی، محو، تیز.
نمی‌شد پریستِ دیگری باشند چون یک داب رنگ حمل می‌کند و از مقصدش خبر ندارد — و دقیقاً به همین
دلیل حالا تقریباً رایگان درآمدند: موتور قلم‌مو از قبل همان چیزی را می‌سازد که لازم دارند،
یک **ماسک پوشش** که نوک و جریان و فاصله و فشار در آن اعمال شده. دوج و برن **محدودهٔ تن**
دارند (سایه/میانی/روشنایی، به‌صورت گاوسی نه باند — لبهٔ باند روی گرادیان خط می‌اندازد) و
**گاما** را جابه‌جا می‌کنند نه روشنایی را، چون جمعِ ثابت ته بازه له می‌کند.

**و یک ادعای غلط دیگرِ خودم.** نوشته بودم «Pathfinder نیست». از موج ۶ هست، روی `Path.op`
اسکیا، و از شیت وکتور در دسترس — با Divide و Outline. آنچه واقعاً کم بود **تست** بود: هیچ
چیزی بررسی نمی‌کرد آن عملیات چه *ناحیه‌ای* برمی‌گرداند. حالا `core:vector` یک پیاده‌سازیِ
مستقل دارد که اوراکلش است، و یک ایراد واقعی هم در خودش پیدا و رفع شد — لبه‌های منطبق، که
شکلِ متحدشده با خودش را ناپدید می‌کرد.

**تست شد.** `FaceTest` (۱۵ ادعا روی هندسه، بدون مدل)، `ToneBrushTest` (۱۴)،
`PathfinderTest` (۱۳)، `PathfinderOracleTest` (۵ مقایسهٔ پوشش با اسکیا). `./gradlew build` سبز.

**ممیزی: ۷۴۰ هست / ۱۵۰ نیمه / ۴۱۳ نیست.**

## جلسهٔ جفت رنگ، انتخاب سریع و ماسک سریع

**جفت رنگ پیش‌زمینه/پس‌زمینه — که نبودش رابط را شکل می‌داد، نه فقط کم بود.** هر شیتی که
رنگ لازم داشت رنگِ خودش را داشت: قلم‌مو یکی، شیت پر کردن سه سواچ ثابت، ابزار شکل یکی دیگر.
پس رنگی که یک‌جا انتخاب می‌شد از جای دیگر در دسترس نبود — و **همین** دلیلِ آن سواچ‌های ثابت
بود، نه اینکه سواچ ثابت چیز خوبی باشد. جابه‌جایی و پیش‌فرض (X و D فتوشاپ) هم تزئین نیستند:
ماسک‌زدن یک تناوبِ پیوسته بین اضافه و کم کردن است و با یک رنگ یعنی هر چند ثانیه باز کردن
انتخابگر.

**انتخاب سریع، که سؤال بهتری می‌پرسد.** عصای جادویی می‌پرسد «کدام پیکسل‌ها شبیه *این یکی*
هستند» و این روی عکس سؤال غلطی است: یک پیراهن پنجاه سایه دارد و یک نمونه روی یکی از آن‌ها
می‌افتد، پس تلورانس آن‌قدر بالا می‌رود تا انتخاب به دیوار نشت کند. این می‌پرسد «کاربر روی
چه ناحیه‌ای کشید» و مدل رنگ را از **کلِ** ضربه می‌سازد — با خوشه‌بندی، نه میانگین، چون
میانگینِ یک پیراهنِ راه‌راه رنگی است که هیچ‌جای عکس وجود ندارد.

**و یک ایراد واقعی که تست پیدا کرد.** اول هزینهٔ لبه را در طول مسیر **جمع** می‌کردم. یعنی
یک گرادیانِ ملایمِ بلند به‌اندازهٔ یک لبهٔ تیز گران تمام می‌شد، و انتخاب وسط یک دیوارِ
سایه‌دار بی‌هیچ دلیلِ دیدنی می‌ایستاد. درستش **مسیرِ گلوگاهی** است: بیشینهٔ سدها در طول
مسیر، نه جمعشان. سؤالی که واقعاً مهم است این است که «آیا هرگز سدی بین اینجا و ضربه بود» و
جوابش نباید به فاصله بستگی داشته باشد.

**ماسک سریع، که بدون جفت رنگ ممکن نبود.** تعریفش همان است: سفید اضافه می‌کند، مشکی کم.
دلیل وجودش هم چیزی است که کمتر گفته می‌شود — **مورچه‌ها پوشش جزئی را نشان نمی‌دهند.** لبهٔ
نرم، ماسک گرادیانی، و مرزِ نرمی که هر ابزارِ این اپ می‌سازد، همه به‌صورت یک خط روی مرزِ
پنجاه‌درصد درمی‌آیند و کاربر دربارهٔ نیمی از اطلاعات حدس می‌زند. ماسک سریع خودِ پوشش را
می‌کشد؛ و همین که دیدنی شد، قابلِ نقاشی هم هست — با نوکِ نرم و جریان و فشار، که همان چیزی
است که یک انتخاب را از «خوب» به «دقیق» می‌برد.

و مثل قلم‌موهای تُنی، هیچ ماشین‌آلاتِ تازه‌ای لازم نداشت: `BrushRasterizer.coverageOf` حالا
همان ماسکِ پوششی را که آن‌ها می‌خوانند به ماسک سریع هم می‌دهد.

**تست شد.** `QuickSelectTest` (ده ادعا روی خاصیت، از جمله «یک تغییرِ رنگیِ یکسان وقتی
تدریجی است رد می‌شود و وقتی ناگهانی است نه»)، `PaletteTest` (هفت). `./gradlew build` سبز.

**ممیزی: ۷۴۴ هست / ۱۵۰ نیمه / ۴۰۹ نیست.**
