# DECISIONS — تصمیم‌های معماری

قالب هر ورودی: **مسئله / گزینه‌ها / انتخاب / دلیل**. تصمیم‌هایی که با سند جامع تفاوت دارند با ⚠️
علامت خورده‌اند؛ برای هرکدام دلیل و هزینهٔ بازگشت نوشته شده.

---

## ۱. ⚠️ زبان هسته: Kotlin به‌جای C++/NDK

**مسئله.** سند (§۴.۴) هستهٔ رستر، هندسه و متن را در C++20 با NDK می‌خواهد و پل JNI نازک.

**گزینه‌ها.** (الف) بازنویسی هسته در C++؛ (ب) نگه‌داشتن هستهٔ Kotlin موجود؛ (پ) مهاجرت تدریجی
ماژول‌به‌ماژول.

**انتخاب.** فعلاً (ب)، با (پ) به‌عنوان مسیر پیش‌رو.

**دلیل.** هستهٔ Kotlin امروز کامل و تست‌شده است: ۲۷ حالت ترکیب، ۲۲ لایهٔ تنظیم، موتور قلم، بردار،
مش سه‌بعدی با PBR و رستریزهٔ نرم‌افزاری. بازنویسی در C++ نه یک اصلاح که ساخت دوباره از صفر است و
هیچ‌یک از معیارهای پذیرش سند را بهتر نمی‌کند — کیفیت خروجی یکسان می‌ماند و فقط سرعت عوض می‌شود.
هزینهٔ واقعی این تصمیم دو چیز است و باید صریح گفته شود: **بودجهٔ ۱۶ میلی‌ثانیهٔ پاسخ ابزار روی
بوم‌های بزرگ**، و **بوم ۱۶۰۰۰×۱۶۰۰۰** که بدون کاشی‌بندی نیتیو در دسترس نیست. اگر اندازه‌گیری روی
دستگاه نشان دهد این دو بودجه رد می‌شوند، ترتیب مهاجرت باید این باشد: اول `core:imaging` (حلقه‌های
داغ فیلتر)، بعد `core:render` (ترکیب)، و متن در آخر.

---

## ۲. ⚠️ shaping متن: استک اندروید به‌جای HarfBuzz بسته‌بندی‌شده

**مسئله.** قاعدهٔ ۲ سازنده می‌گوید shaping هرگز به سیستم‌عامل واگذار نشود، چون در مسیر GPU و
سه‌بعدی `Canvas.drawText` در دسترس نیست و دو مسیر متفاوت، خروجی دوبعدی و سه‌بعدی را از هم جدا
می‌کند.

**گزینه‌ها.** (الف) بسته‌بندی HarfBuzz در NDK؛ (ب) استفاده از `android.graphics` که خودش زیر
پوسته HarfBuzz و ICU است.

**انتخاب.** فعلاً (ب).

**دلیل و اعتراف.** استدلال سند درست است و این تصمیم یک بدهی است، نه یک ترجیح. آنچه امروز خطر را
کم نگه می‌دارد این است که **هر دو مسیر از یک منبع تغذیه می‌شوند**: مسیر سه‌بعدی هم گلیف‌ها را از
همان `android.graphics.Paint.getTextPath` می‌گیرد، پس اتصال حروف در دوبعدی و سه‌بعدی امروز یکی
است. چیزی که واقعاً از دست می‌رود کنترل است: نیم‌فاصله در سطح مدل متن، کشیده با قواعد فارسی، و
ویژگی‌های OpenType که سند در §۵.۲.ب فهرست کرده — هیچ‌کدام بدون دسترسی مستقیم به `hb_buffer`
شدنی نیستند. **این باید اولین بدهی‌ای باشد که تسویه می‌شود.**

---

## ۳. ⚠️ موتور سه‌بعدی: رستریزهٔ اختصاصی به‌جای Filament

**مسئله.** سند Filament را انتخاب کرده (§۴.۴) با IBL، مدل‌های متریال آماده و دوربین فیزیکی.

**گزینه‌ها.** (الف) افزودن Filament؛ (ب) نگه‌داشتن `core:mesh` موجود.

**انتخاب.** فعلاً (ب).

**دلیل.** `core:mesh` امروز مثلث‌بندی گوش‌بری، اکسترود با بِوِل، Cook-Torrance/GGX، محیط تحلیلی
استودیویی و تون‌مپ ACES دارد و در تست‌های تصویری طلا و کروم درست درمی‌آیند. Filament چیزهایی
می‌دهد که این ندارد و ساختنشان گران است: **IBL از HDRI واقعی**، Clear Coat/Anisotropic/Sheen،
دوربین فیزیکی، و `cmgen`. هرکدام از این‌ها یک قابلیت مشخص از §۶ سند است که تا Filament نیاید
نخواهد آمد. تصمیم درست احتمالاً افزودن Filament در فاز دو است، نه هرگز.

---

## ۴. ⚠️ فرمت بومی: `.pxl` به‌جای `.kbd`

**مسئله.** سند `.kbd` را به‌عنوان کانتینر ZIP با `document.json` تعریف کرده و می‌گوید قرارداد فرمت
مهم‌ترین خروجی جلسهٔ اول است چون بعداً تغییرش گران‌ترین کار پروژه است.

**انتخاب.** ساختار امروز **دقیقاً همان است** — کانتینر ZIP با سند سریال‌شده و پوشهٔ دارایی — فقط
پسوند و نام فرق دارد. تغییر نام یک تغییر یک‌خطی است تا وقتی سند هنوز منتشر نشده.

**دلیل تعویق.** هویت محصول (پیکسل‌لب در برابر کاربیست دیزاین) هنوز تصمیم کاربر است؛ تغییر پسوند
قبل از آن تصمیم یعنی احتمالاً دو بار تغییرش می‌دهیم.

---

## ۵. رنگ تأکید: کهربایی، و فیروزه‌ای در نقش انتخاب

**مسئله.** در همین پروژه، پیش‌تر فیروزهٔ `#22D3C5` به‌عنوان رنگ تأکید انتخاب شده بود. سند رنگ
برند را کهربایی `#E8A33D` می‌گذارد و فیروزه‌ای `#3DCCC0` را برای انتخاب و ماسک.

**انتخاب.** هر دو، در نقش‌هایی که سند تعیین کرده.

**دلیل.** تعارض ظاهری است نه واقعی: سند فیروزه‌ای را حذف نکرده، نقشش را عوض کرده. نتیجه بهتر از هر
دو حالت قبلی است، چون حالا رنگ ابزار فعال و رنگ پیکسل‌های انتخاب‌شده **از هم قابل تفکیک‌اند** — که
در ویرایشگری با ماسک، یک تفاوت کارکردی است نه سلیقه‌ای.

---

## ۶. بدون گرادیان تزئینی

**مسئله.** نسخهٔ قبلی رابط یک گرادیان روی دکمهٔ اصلی داشت که به‌عنوان «تنها گرادیان اپ» توجیه شده
بود. سند (§۶) گرادیان تزئینی را ممنوع می‌کند.

**انتخاب.** حذف شد؛ دکمهٔ اصلی کهربایی تخت است.

**دلیل.** استدلال سند قوی‌تر از توجیه قبلی است: بوم باید روشن‌ترین و جالب‌ترین چیز صفحه باشد، و
گرادیان روی کنترلی که کاربر روزی پنجاه بار می‌زند تزئین است. سود جانبی: حالا رنگ تأکید همه‌جا دقیقاً
یک معنا دارد.

---

## ۷. داک پنج‌تایی به‌جای نوار ابزار نه‌تایی

**مسئله.** رابط قبلی نه ابزار را در یک نوار افقی اسکرول‌شونده می‌گذاشت.

**انتخاب.** ساختار §۱۳.۵ سند: داک ۶۴dp با پنج ورودی ثابت (`عکس · متن · سه‌بعدی · لایه · خروجی`) و
ریبون بافتار ۸۸dp بالای آن که با داک عوض می‌شود.

**دلیل.** نه ابزار روی صفحهٔ ۴۱۱dp یعنی ۴۵dp برای هرکدام — زیر حداقل لمسی. راه‌حل قبلی (اسکرول) دو
ابزار آخر را پنهان می‌کرد. پنج ورودی بدون اسکرول جا می‌شوند و ابزارها به ریبون می‌روند، جایی که با
کاری که کاربر دارد می‌کند گروه می‌شوند.

---

## ۸. نوار تاریخچه: نشانه، نه بندانگشتی

**مسئله.** ایدهٔ ۷ سند تاریخچه را «نوار فیلم» با بندانگشتی هر گام می‌خواهد.

**انتخاب.** یک نشانهٔ کوچک به‌ازای هر گام، با پرش با ضربه. بدون تصویر.

**دلیل.** بندانگشتی هر گام یعنی یک رندر کامل سند به‌ازای هر ورودی تاریخچه؛ روی بوم ۴۰۰۰ پیکسلی این
ثانیه‌ها کار برای تصویری ۴۰dp است. نوار نشانه، کارِ اصلی را می‌کند (پرش به گام) و دروغ نمی‌گوید.
شاخه‌بندی (کشیدن به پایین) هنوز نیست.

---

## ۹. مدل جداسازی: فقط سنگین و حرفه‌ای

**مسئله.** انتخاب بین مدل‌های سبک (۵–۱۵ مگابایت) و سنگین.

**انتخاب.** BiRefNet fp16 (۴۶۷ مگابایت)، با مسیر کلاسیک به‌عنوان پیش‌فرض بدون مدل.

**دلیل.** خواستهٔ صریح کاربر، و هم‌راستا با §۱۰.۲ سند که BiRefNet را مسیر محلی حذف پس‌زمینه
می‌گذارد. RMBG-2.0 رد شد: دانلودش پشت احراز هویت است و مجوزش غیرتجاری.

---

## ۱۰. رشته‌های رابط هنوز در کد

**مسئله.** فهرست «هرگز نکن» سند می‌گوید رشتهٔ رابط hardcode نشود — همه در `strings.xml`.

**وضعیت.** ❌ نقض‌شده. حدود ۹۰۰ رشتهٔ فارسی در ۴۵ فایل Kotlin.

**دلیل تعویق، نه توجیه.** این یک بدهی واقعی است. انتقالش مکانیکی است و ریسک رگرسیونش در همان
مکانیکی بودن است: ۹۰۰ جایگزینی دستی در فایل‌هایی که تست تصویری‌شان فقط «چیزی کشیده شد» را چک
می‌کند. تصمیم: در یک پاس مستقل انجام شود، فایل‌به‌فایل، نه به‌عنوان بخشی از کار دیگری.
