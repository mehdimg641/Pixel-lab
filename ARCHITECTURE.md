# ARCHITECTURE

معماری امروز مخزن. برای تفاوت‌ها با سند جامع و دلیل هرکدام، `DECISIONS.md`؛ برای نگاشت ماژول به
بخش سند، `SPEC-MAP.md`.

## جهت وابستگی‌ها

وابستگی‌ها فقط به سمت پایین‌اند. هیچ ماژول هسته‌ای اندروید را نمی‌بیند، و به همین دلیل با JUnit5
روی JVM تست می‌شود.

```
                        app:android
                             │
                      engine:android          ← تنها لایه‌ای که Android SDK را می‌بیند
                             │
   ┌─────────┬──────────┬────┴─────┬──────────┬──────────┐
core:editor core:codec core:paint core:ai  core:canvas core:mesh
   │            │          │         │          │          │
   └────────┬───┴──────────┴─────────┴──────────┘          │
      core:render ─── core:imaging                    core:vector
            │                                              │
      core:text ─── core:fonts                             │
            │                                              │
            └──────────────── core:model ──────────────────┘
```

- `core:model` — سند، لایه، افکت، تبدیل. بدون رفتار، فقط داده و سریال‌سازی.
- `core:text` — چیدمان متن، اندازه‌گیری، جهت. shaping از استک اندروید می‌آید (`DECISIONS.md` §۲).
- `core:fonts` — اسکن، گروه‌بندی، جست‌وجو و پیش‌نمایش فارسی فونت‌های کاربر.
- `core:render` — گراف رندر، ۲۷ حالت ترکیب، ۲۲ لایهٔ تنظیم، Layer Styles، شیدرها.
- `core:imaging` — فیلترهای پیکسلی: تاری، شارپ، نویز، سایه/هایلایت، هیستوگرام، بافت رویه‌ای.
- `core:paint` — موتور قلم و انتخاب پیکسلی.
- `core:vector` — مسیر بزیه، بولین، آفست، پروفایل عرض، SVG.
- `core:mesh` — متن به هندسه: مثلث‌بندی، اکسترود، بِوِل، PBR، رستریزهٔ نرم‌افزاری.
- `core:canvas` — خط‌کش، راهنما، چسبندگی، viewport.
- `core:codec` — خواندن و نوشتن فرمت‌ها، پروژه، PSD، PDF.
- `core:editor` — حالت ویرایشگر، تاریخچه، ابزار فعال، کتابخانه.
- `core:ai` — قرارداد مدل و مسیر کلاسیک جایگزین.
- `engine:android` — GL، کدک‌های پلتفرم، ONNX Runtime، متن به سه‌بعدی، بودجهٔ فریم.
- `app:android` — سیستم طراحی، خانه، ویرایشگر، پنل‌ها.

## جریان داده — از انگشت تا پیکسل

```
لمس روی بوم
   ↓  EditorCanvas  (تبدیل مختصات صفحه → بوم)
   ↓  EditorViewModel.onGesture  (مسیریابی به ابزار فعال)
   ↓  Editor  (جهش سند، ثبت گام تاریخچه)
   ↓  EditorState  ← Compose از این می‌خواند و بازترکیب می‌شود
   ↓  RenderGraph  (ترتیب لایه، ماسک، افکت، تنظیم)
   ↓  engine:android  (GL، شیدرها)
   ↓  پیکسل روی صفحه
```

دو نکته دربارهٔ این مسیر:

**تاریخچه یکی است، نه دو تا.** پیکسل‌های نقاشی‌شده جزو سند نیستند، ولی روی همان پشتهٔ گام‌ها
می‌نشینند (`EditorViewModel.Step`). دو پشتهٔ جدا باعث می‌شد وقتی کاربر بین نقاشی و جابه‌جایی
عوض می‌کند، undo یک گام را بپرد.

**اندازه‌گیری یکی است، نه دو تا.** همان `LayerMeasure` که ویرایشگر برای چسبندگی و برخورد استفاده
می‌کند، دستگیره‌های انتخاب را هم می‌کشد. پیاده‌سازی دوم برای دستگیره‌ها، راه استانداردِ خراب‌شدن
این بخش است.

## مرز اندروید

`engine:android` تنها ماژولی است که `android.*` را import می‌کند. هرچه بالای آن است Compose است و
هرچه پایینش، Kotlin خالص. این مرز، جای همان مرز JNI در سند است (§۴.۴) — با این تفاوت که امروز
تبادل داده داخل یک فرآیند JVM است، پس بافر پیکسلی از مرز رد می‌شود. اگر هسته به C++ مهاجرت کند،
قاعدهٔ «فقط handle، هرگز بافر پیکسلی» باید همین‌جا اعمال شود.

## سیستم طراحی

در `app/android/src/main/kotlin/ir/pixellab/app/Theme.kt` و `Components.kt`. توکن‌ها دقیقاً از §۶
سند می‌آیند و هیچ فایل دیگری اجازهٔ نوشتن رنگ یا اندازهٔ خام ندارد. جدول تطابق در `SPEC-MAP.md`.

## تست

| ماژول | چارچوب | چرا |
|---|---|---|
| ماژول‌های خالص | JUnit5 + kotest | روی JVM، بدون شبیه‌ساز |
| ماژول‌های اندرویدی | JUnit4 + Robolectric با `graphicsMode=NATIVE` | Skia واقعی، نه stub |
| رابط | `ScreenshotTest` — رندر واقعی به PNG | یک کنترل که کامپایل می‌شود و هیچ نمی‌کشد را فقط تصویر می‌گیرد |

`allWarningsAsErrors = true` در هر ماژول.
